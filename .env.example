# ------------------------------
# Payload CMS Configuration
# ------------------------------
# Secret string used to encrypt/decrypt cookies and tokens.
# You can generate one using: openssl rand -base64 32
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Database Connection String (MongoDB or PostgreSQL)
# Example Postgres: postgres://user:password@localhost:5432/your-database-name
# Example Mongo: mongodb://127.0.0.1/your-database-name
DATABASE_URI=postgres://user:password@localhost:5432/resumebuilder

# ------------------------------
# Next.js & Public URLs
# ------------------------------
# The URL of your Payload instance (used for redirects and emails)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# ------------------------------
# Email Configuration (Optional but recommended for Auth)
# ------------------------------
# SMTP_HOST=smtp.resend.com
# SMTP_USER=resend
# SMTP_PASS=
# SMTP_PORT=587
# SMTP_FROM_NAME="ResumeBuilder Team"
# SMTP_FROM_ADDRESS="onboarding@resumebuilder.com"

# ------------------------------
# Storage / Media (If using S3 or similar, otherwise local is default)
# ------------------------------
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_BUCKET=
# S3_REGION=