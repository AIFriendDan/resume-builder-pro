export const metadata = {
  title: 'Resume Workspace | Resume Builder Pro',
  description: 'Edit, tailor, and export resumes directly in the browser.',
};

export default function ResumeLayout({ children }) {
  return children;
}
